management:
  endpoints:
    enabled-by-default: true

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://${HOST_NAME:localhost}:5432/jobs
    username: user
    password: password
